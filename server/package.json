{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node build/index.js",
    "start-dev": "env NODE_ENV=development nodemon build/index.js",
    "dev": "concurrently \"npm run build-dev\" \"npm run start-dev\"",
    "seed": "node build/database/seed.js",
    "seed-dev": "env NODE_ENV=development npm run seed",
    "build": "babel src -d build",
    "build-dev": "env NODE_ENV=development babel -w src -d build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.4"
  },
  "devDependencies": {}
}
